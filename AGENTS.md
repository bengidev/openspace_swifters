# OpenSpace (iOS) — Agent Notes

## Agent skills

### Issue tracker

GitHub Issues di `bengidev/openspace_swifters` via `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context: `CONTEXT-MAP.md` di root menunjuk ke per-feature `CONTEXT.md` di bawah `OpenSpace/OpenSpace/Features/<Feature>/` dan ke cross-cutting `OpenSpace/OpenSpace/Shared/CONTEXT.md`. See `docs/agents/domain.md`.
